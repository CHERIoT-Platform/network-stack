/**
 * This header is included in FreeRTOS code at the end of a packet structure
 * definition.  It expands to the GCC attribute for a packed structure.
 *
 * Note: This must contain the trailing semicolon.
 */
__attribute__((packed));
